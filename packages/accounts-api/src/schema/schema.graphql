type JWT {
  valid: Boolean!,
  token: String,
  first_name: String
}

type Query {
  hello(name: String): String
  find(email: String): String
}

type Mutation {
  hello(name: String): String
  authenticate(email: String, password: String): JWT
}