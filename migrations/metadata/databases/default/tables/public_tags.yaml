table:
  schema: public
  name: tags
select_permissions:
  - role: common_user
    permission:
      columns:
        - id
        - name
        - taggings_count
        - label
        - kind
      filter: {}
