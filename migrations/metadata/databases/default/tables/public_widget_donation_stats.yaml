table:
  schema: public
  name: widget_donation_stats
select_permissions:
  - role: anonymous
    permission:
      columns:
        - widget_id
        - stats
      filter: {}
  - role: user
    permission:
      columns:
        - widget_id
        - stats
      filter: {}
