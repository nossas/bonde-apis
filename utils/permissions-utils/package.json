{
  "version": "0.0.1",
  "name": "permissions-utils",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p .",
    "postinstall": "yarn run build",
    "clean": "rm -rf node_modules dist",
    "lint": "eslint . --ext ts,tsx --fix",
    "test": "jest --passWithNoTests",
    "coverage": "jest --coverage"
  }
}
